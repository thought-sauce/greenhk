class MapController < ApplicationController
  def index
    # @districts = District.all
    # @subdistricts = Subdistrict.all
    # @recycle_bins_markers = RecycleBin.all.map{|bin| 
    #   {
    #     :id => bin.id,
    #     :add => bin.add,
    #     :add_eng => bin.add_eng,
    #     :lat => bin.lat,
    #     :long => bin.long
    #   }
    # }
  end
end
